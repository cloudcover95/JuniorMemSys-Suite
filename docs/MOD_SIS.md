# MemSys vs Module-SIS / Jolt

Palace commit: `junior_memsys_suite/lattice/msis_commit.py` (toy `Az=c`).
Ternary argument + toy VM + toy signature API + bench: `cloudcover95/JuniorLLM` package `lattice_zk`.

Jolt numbers stay a reference line. We do not claim 2M RV cycles/s or 128-bit PQ SNARKs.
